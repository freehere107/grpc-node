syntax = "proto3";

package web3;

service EthWeb3 {
    rpc SignedTypeMsg (TypeMsg) returns (Signed) {
    }
}

message TypeMsg {
    string msg = 1;
    string private_key = 2;
}

message Signed {
    string message = 1;
}
